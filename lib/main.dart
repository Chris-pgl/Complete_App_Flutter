import 'package:flutter/material.dart';
import 'package:flutter_last_app/infotourpage.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: InfoTourPage());
  }
}
